# Architecture

Flask is the canonical standalone API. The hosted portfolio has a Worker adapter to the same normalized contract. Client ranking is a pure module over a small catalog result sample. No collaborative filtering, learned taste model, or invented synopsis.
